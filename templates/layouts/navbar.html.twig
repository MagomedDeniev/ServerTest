<nav class="navbar navbar-expand-lg bg-light">
    <div class="container-xl">
        <a class="navbar-brand d-flex align-items-center" href="{{ path('app_home') }}">
            <img src="{{ asset('build/images/transparent_logotype.png') }}" alt="Logo" width="50" height="50" class="d-inline-block align-text-top">
            <span class="ms-2 fw-bold">ServerTest</span>
        </a>

        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="#">Партнеры</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">О нас</a>
                </li>
            </ul>
        </div>

        {% if app.user %}

            <div class="dropdown">
                <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                    <i class="bi bi-person-fill"></i>
                </a>
                <ul class="dropdown-menu">
                    <li><a class="dropdown-item" href="{{ path('user_profile', {'username': app.user.userIdentifier}) }}">Профиль</a></li>
                    <li><a class="dropdown-item" href="{{ path('user_edit') }}">Редактировать</a></li>
                    <li><a class="dropdown-item" href="{{ path('auth_logout') }}">Выйти</a></li>
                </ul>
            </div>

        {% else %}

            <a class="btn btn-light me-2" href="{{ path('auth_login') }}">Войти</a>

            <a href="{{ path('auth_register') }}" class="btn btn-primary">
                Зарегистрироваться
            </a>

        {% endif %}

    </div>
</nav>
